package model.admin;

public class DepartmentA extends Admin {
    public DepartmentA(String userid, String password, Permission managePermission) {
        super(userid, password, managePermission);
    }
}
