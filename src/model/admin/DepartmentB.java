package model.admin;

public class DepartmentB extends Admin {
    public DepartmentB(String userid, String password, Permission managePermission) {
        super(userid, password, managePermission);
    }
}
